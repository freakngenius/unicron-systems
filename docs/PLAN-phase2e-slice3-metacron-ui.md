# PLAN — Phase 2E Slice 3 · Metacron UI

Phase 2E Slices 1, 2, and 4 already landed on main: the DB column `pathfinder.organizations.status` carries the 6-state machine (`setting_up → first_run → ranking → awaiting_threshold | ready_to_view → operator_viewed`), the Pathfinder POST/GET writes/reads it, and the proxy `unicron-platform/api/internal/organizations.ts` passes the value through unchanged. Slice 3 is the operator-facing surface in Metacron: the legacy `OrgStatus` union (`active | onboarding | paused`) is out of sync with the DB and the Customers tab still renders against it. This worktree updates the contract, the Customers grid badge, and the Customer Detail header so operators can see exactly which phase of the onboarding loop each org is in and only navigate into Pathfinder once the org has crossed the verified-leads threshold.

- Replace `OrgStatus` in `src/lib/contracts/customers.ts` with the 6-state union; keep field name `status`.
- Update `STATUS_TONE` in `src/views/CustomersView.tsx` to 6 entries (muted → emerald progression) and render the new label.
- In `src/views/CustomerDetailView.tsx`: render a status badge next to the org id (same tone map), and gate the Open Pathfinder button — enabled only at `ready_to_view` / `operator_viewed`, otherwise a greyed `<button>` with no `href` and a `title=` reason. Same `data-testid` either way. Extract `STATUS_LABEL` + `STATUS_TONE` into a small inline helper at the top of CustomerDetailView (under 300-line ceiling).
- Add test coverage: extend `src/lib/customersClient.test.ts` to round-trip all 6 statuses through `listCustomerOrgs`. New `src/views/CustomerDetailView.test.tsx` asserts badge text + button enabled/disabled state for each status.
- Run `npm run lint`, `npm run typecheck`, `npm run test`; open PR; auto-merge on CI + 4 Vercel previews green; codex review skipped (usage limit). On merge, move Metacron kanban card `35c785c67e7281348b25de912f013a71` to Deployed and append `Implemented at <sha> · merged at <iso> · PR #<num> · Slice 3 closes Phase 2E.`
