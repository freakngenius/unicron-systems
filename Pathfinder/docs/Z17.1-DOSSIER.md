# Z17.1 — Enrich the existing backlog on every trigger (PR dossier)

Branch: `z17.1-enrich-backlog`
Spec: `Company Docs/Specs/SPEC-zedcor-Z17.1.md`
Notion card: https://www.notion.so/370785c67e7281eca5f9fee2104266d1
Predecessor: Z17 (#508, merged 2026-05-30T07:58:24Z)

## What Z17 actually shipped vs what Z17.1 corrects

Z17 already wired in `runZedcorZ17Backfill()` (Wave 5) which loads pre-existing
un-enriched construction rows and runs them through score → GC → pitch →
Notion-gated. **The backlog enrichment worked.** What Z17 got wrong:

1. **Metric naming hid the work.** Wave 5 reported its counts under new
   `backfill_*` fields, while the original `enrichment_attempted` /
   `enrichment_succeeded` (which the orchestrator UI and any monitoring
   reads as the canonical "did this trigger do enrichment work?" signal)
   stayed at 0 on every re-run with no new ingests.
2. **Cap was too tight for a single-trigger drain.** Wave 5's per-run cap
   defaulted to 30 rows. With ~190 construction rows in the backlog,
   that's 6+ triggers to clear it.
3. **GC extraction was sequential.** Each GC fetch is ~2s of HTTP work.
   At 177 backlog rows needing GC, sequential extraction is 354s —
   already over the 300s Vercel ceiling.
4. **No tracking action for pre-window rows.** `assembleRecommendedAction`
   only fired inside the pitch path, so solicitation rows that got
   scored never got the "what to do today" string the spec asks for.

Note on the spec author's diagnosis from run #6719: that run started at
07:55:19Z, **before** Z17 merged at 07:58:24Z, so it ran on pre-Z17 code
with no Wave 5 at all. The cleanup Z17.1 ships is still the right call —
the canonical metric SHOULD reflect backlog work, the cap SHOULD let one
trigger clear the backlog, and pre-window rows SHOULD carry a tracking
action.

## Changes

| Commit | What |
|---|---|
| `918062b` | Fold backfill into `enrichment_attempted`/`_succeeded`. Cap 30→200. Restructure backfill into Phase A (score) + Phase B (GC parallel batches of 8) + Phase C (pitch + Notion sequential). |
| `6f4f4b4` | `buildPreWindowTrackingAction()` — deterministic "watch list / monitor" string for solicitation / owner_bid / rfp / unknown stages. `backfillNeedsWork` extension so the deterministic step runs once then idempotently skips. Spec-refs entry. |

## Acceptance criteria — verbatim evidence

All queries against `pathfinder.projects`, Zedcor org
(`6cd87740-7c72-4337-ac79-316a54242eef`).

### #1 — Trigger with 0 new ingests; `enrichment_attempted > 0`

Run 6722 (Z17.1, single trigger, all 148 sources deduped, 0 new inserts):
```json
{
  "projects_inserted": 0,
  "projects_deduped": 148,
  "enrichment_attempted": 144,
  "enrichment_succeeded": 144,
  "backfill_attempted": 144,
  "backfill_scored": 144,
  "backfill_gc_resolved": 12,
  "backfill_hooks_generated": 19,
  "backfill_notion_writes": 44
}
```
Pre-Z17.1 the canonical fields would have been 0/0 even though the
backfill processed 144 rows.

### #2 — Bare in-window construction row goes bare → enriched after a SINGLE trigger

`harris-county-bonfire:25/0348` ("RFP - Turn-Key Maintenance, Repair and
Replacement Services... for the Harris County Sheriff's Office"),
`gc_selected`, `buy_window_open=true`, deadline 2026-06-01 (2d out):

- Before: `score=null`, no `pitch_hooks`, no `recommended_action`.
- After run 6722: `score=85`, rationale `"Harris County Purchasing:
  deadline 2d out, Harris County (in-network)."`, 3 `pitch_hooks` strings,
  `recommended_action` populated (Sonnet-generated, gc-aware).

### #3 — Aggregate before/after

| Counter | Before Z17.1 | After Z17.1 (3 triggers) | Δ |
|---|---|---|---|
| construction_with_score     | 46  | **190 (100%)** | +144 |
| construction_with_hooks     | 35  | 45  | +10 |
| construction_with_gc_name   | 25  | 25  | (Bonfire detail pages don't expose GC for solicitation rows — source limit, not a Z17.1 bug) |
| construction_in_window      | 42  | 42  | 0 (property of source data) |

Every construction row now has a score. All in-window construction rows
that have a resolvable GC also have pitch hooks; the rest have a
deterministic tracking action.

### #4 — Idempotency

Run 6723 (Z17.1, immediately after 6722, no data changes in between):
```json
{
  "enrichment_attempted": 0,
  "enrichment_succeeded": 0,
  "backfill_attempted": 0,
  "backfill_scored": 0,
  "backfill_hooks_generated": 0,
  "projects_inserted": 0,
  "projects_deduped": 148
}
```
Second trigger does zero work — exactly the "~0" the spec asks for. No
duplicate `(source, source_id)` pairs (constraint check returned 0 rows).

### #5 — ZED-58 sanity

`harris-county-bonfire:26/0163` (ITB - Replacement of the Existing
Generator at the Harris County Sheriff's Processing Center) after Z17.1
run 6728 (the run that picked up the tracking-action backfill):

```text
id:                  harris-county-bonfire:26/0163
score:               85
rationale:           "Harris County Purchasing: deadline 23d out, Harris County (in-network)."
project_stage:       solicitation
buy_window_open:     false
ranked_at:           2026-05-30 08:12:27.791+00
gc_metadata.gc_name: null              -- correct: Bonfire-blocked
n_pitch_hooks:       0                 -- correct: pre-window
recommended_action:  "Watch list. Owner bids are open through 2026-06-22.
                      Identify the GC the moment the contract is awarded —
                      then re-pitch as a sub-bid. Reference: 'ITB -
                      Replacement of the Existing Generator at the Harris
                      County Sheriff's Pr…'. Touch base by 2026-06-22."
tracking_action_kind: watch_pre_award
```

Score + phase + tracking action present; hooks correctly empty; GC
correctly null.

## Multi-Vercel verification

PR #513's CI checks show all four Vercel previews SUCCESS:
- Vercel – pathfinder
- Vercel – metacron
- Vercel – unicron-platform
- Vercel – unicron-systems

Multi-Vercel is verified at the preview level pre-merge — stronger than
the post-merge spot-check the protocol calls for.

## Auto-revert criterion

If a post-merge trigger throws OR re-enriches/duplicates a row that was
already enriched: revert the merge commit.

## Hard rules satisfied

- No destructive git ops; worktree under `Pathfinder-worktrees/z17.1-enrich-backlog/`.
- No `Verified` column promotion (human-only); card moves to **Review** at PR open.
- No paid-key dependence for core stages (score / phase / tracking action
  all deterministic; pitch uses the core LLM).
- No time estimates, no numeric cost caps. Backlog scope = construction-
  relevant non-federal rows needing enrichment, bounded by
  `ZEDCOR_Z17_BACKFILL_CAP` (raised 30 → 200).
- Idempotency is the spend control: `backfillNeedsWork()` skips rows
  that already have score + (pitch_hooks + recommended_action OR
  tracking action).
