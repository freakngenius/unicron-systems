# 2026-05-02 — SPEC drift: `agent_run_id` type in SPEC §8

`Company Docs/Specs/SPEC - Agent Console (Metacron).md` §8 documents `unicron.agent_dispatches.agent_run_id` as `uuid` (referencing `pathfinder.agent_runs(id)`). Production reality is `bigint` because `pathfinder.agent_runs.id` has been `bigint` since `Pathfinder/supabase/migrations/0002_tables.sql` shipped — verified during Phase 0.5 (PR #75) via `information_schema.columns` against project `anfihcusvekpovcchpoh`. The Phase 0.5 migration `unicron-platform/supabase/migrations/0001_agent_console.sql` was corrected inline to use `bigint` so the FK could be created (initial `apply_migration` attempt failed with `42804: foreign key constraint "agent_dispatches_agent_run_id_fkey" cannot be implemented … incompatible types: uuid and bigint`). This todo tracks updating the spec text so future readers don't repeat the mismatch. Doc-only fix; bundle into the next docs-sweep PR or fold into the Phase 1 prompt regeneration.
